---
description: Restyle the targeted element with a small, self-contained inline style
---

Apply a focused visual restyle to the element referenced in this handoff.
Prefer an inline `style={{ … }}` prop on the target in `src/App.jsx`, keep the
change minimal, and don't introduce a global stylesheet.
